import { Message } from '../messages/message.entity';
import { Chat } from '../chats/chat.entity';
import { User } from './user.headers';

export const chatEntities = [Message, Chat];
export const mainEntities = [User];
